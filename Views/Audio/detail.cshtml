@model Product
<div>
    <h2>@Model.Name</h2>
    <audio controls width="500">
        @*<source src="/videos/@Model.Url" type="video/@Model.Type" />*@
        <source src="/audio/play/@Model.Id" type="audio/flac" />
    </audio>
</div>