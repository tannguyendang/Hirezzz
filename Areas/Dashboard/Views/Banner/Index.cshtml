@model List<Banner>
<button type="button" class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#exampleModal">
    Add
</button>
<!-- Modal -->
<div class="modal fade" id="exampleModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <form method="post" action="/dashboard/banner/add" class="modal-content" enctype="multipart/form-data">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLabel">Modal title</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <div>
                    <label>Name</label>
                    <input type="text" name="name" />
                </div>
                <div>
                    <label>Image</label>
                    <input type="file" name="f" accept="image/*" />
                </div>
                <div>
                    <label>Type: </label>
                    <select name="type">
                        <option value="0">Slide</option>
                        <option value="1">Ad_Left</option>
                        <option value="2">Ad_Right</option>
                    </select>
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                <button class="btn btn-primary">Save changes</button>
            </div>
        </form>
    </div>
</div>
<table class="table table-bordered">
    <thead class="table-dark">
        <tr>
            <td>Id</td>
            <td>Name</td>
            <th>Image</th>
            <th>Type</th>
            <th>Delete</th>
            <th>Edit</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr val="@item.Id">
                <td>@item.Id</td>
                <td>@item.Name</td>
                <th>@item.ImageUrl</th>
                <th>@item.Type</th>
                <td>
                    <a href="/dashboard/banner/delete/@item.Id"><i class="fa-sharp fa fa-trash"></i></a>
                </td>
                <td>
                    <a href="/dashboard/banner/edit/@item.Id"><i class="fa-sharp fa fa-wrench"></i></a>
                </td>
            </tr>
        }
    </tbody>
</table>